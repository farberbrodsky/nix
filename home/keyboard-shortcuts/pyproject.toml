[project]
name = "keyboard_shortcuts"
version = "1.0"
description = "A minimal pyproject.toml"
authors = []
requires-python = ">=3.13"
dependencies = []

[project.scripts]
mishakeys = "keyboard_shortcuts:main_cli"
